spring:
  application:
    name: zuul-proxy-server

server:
  port: 8765

zuul:
  routes:
    university-service: /uni/**
    url: lb://university-service




eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}