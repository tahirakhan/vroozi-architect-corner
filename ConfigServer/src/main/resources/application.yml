
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tahirakhan/vroozi-cloud-config-repo
          username: 
          password: 
          default-label: master
  application:
    name:config-server
server:
  port: ${port:8888}
